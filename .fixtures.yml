fixtures:
  repositories:
    stdlib:
      repo: "git://github.com/puppetlabs/puppetlabs-stdlib.git"
      ref: "3.7.0"
    apt:
      repo: "git://github.com/puppetlabs/puppetlabs-apt.git"
      ref: "2.2.2"
    logrotate:
      repo: "git://github.com/voxpupuli/puppet-logrotate.git"
      ref: "1.4.0"
  symlinks:
    mongodb: "#{source_dir}"
